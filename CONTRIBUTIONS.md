# Contributions

This repo is a backend / verification study built **on top of** a Tiny
Tapeout RTL submission by Robert Solomon Saab (Discord `.djharvey`).
The RTL (`src/project.v`) is Robert's original submission, untouched
in this fork. A one-cycle CHECK-state pipeline modification was
prototyped to attack the 100 MHz SS-corner setup violation but
reverted before push — see [`design_notes.md` § Pipeline Modification
(proposed)](design_notes.md) for the diff and the reason it stayed
out of the shipped RTL. Everything else (physical flow runs,
comparisons, formal verification, tooling, write-ups, the 10-case
cocotb regression) is by the maintainer of this fork.

## Author split

| File / directory | Original author | Notes |
|---|---|---|
| `src/project.v` (`tt_um_range_finder`, `Day_12_solver`) | Robert Solomon Saab | RTL — untouched in this fork; CHECK-state pipeline patch was prototyped and reverted (see `design_notes.md`) |
| `src/tt_um_example.v` | Tiny Tapeout template | placeholder, not synthesised |
| `info.yaml` (project metadata) | Robert Solomon Saab | attribution preserved |
| `test/tb.v`, `test/Makefile`, `test/cocotb.ini` | Robert Solomon Saab | testbench scaffolding — untouched |
| `test/test.py` | **maintainer** (replaces Robert's 1-case stub) | 10-case cocotb regression drawn from `day12_golden_model.py` (3 cases dropped — see `verification_notes.md` for the HW/golden disagreement) |
| `verification_notes.md` | **maintainer** | per-case results.xml table for the 3 dropped cases + candidate root causes |
| Tiny Tapeout flow scaffolding (`.github/workflows/*`, `docs/info.md`, `LICENSE`) | Tiny Tapeout project | upstream |
| `src/aggressive_config.json` | **maintainer (this fork)** | 100 MHz / 80 % density LibreLane override for Task 1 |
| `src/pipelined_config.json` | **maintainer** | 100 MHz / 80 % density override scaffolding for a future pipelined-RTL LibreLane run |
| `runs/aggressive/` (keeper artefacts only) | **maintainer** — generated by LibreLane from Robert's RTL | commit d9f43f5 |
| `pipelined_compare.py` | **maintainer** | three-way baseline / aggressive / pipelined PPA diff scaffolding → `pipelined_compare.md` (pipelined column shows n/a until the run lands) |
| `extract_ppa.py` | **maintainer** | `metrics.json` → `ppa_report.md` |
| `compare_ppa.py` | **maintainer** | baseline vs aggressive diff → `ppa_compare.md` |
| `ppa_report.md`, `ppa_compare.md` | **maintainer** | generated write-ups |
| `day12_golden_model.py` | **maintainer** | Python reference model mirroring the AXI byte protocol, used as HW/SW equivalence argument |
| `formal/equiv_baseline.ys`, `formal/equiv_aggressive.ys` | **maintainer** | Yosys sequential equivalence check scripts |
| `formal/strip_fillers.py` | **maintainer** | preprocessor stripping `sky130_ef_sc_hd__{decap,fill,tap,diode}` cells from post-synth netlists |
| `formal/sky130_buf_stubs.v` | **maintainer** | behavioural stubs for buffer / delay / `dfxtp` / `conb` cells so `equiv_induct` can propagate through timing-repair buffers |
| `formal/run_formal.sh` | **maintainer** | Docker wrapper for the yosys flow |
| `formal/README.md` | **maintainer** | methodology write-up + honest account of why 9 of 29 $equiv cells remain unproven by pure k-induction |
| `docs/klayout_*.png` | **maintainer** | KLayout screenshots of the final GDS and the Caravel-style context |
| `README.md` (this repo's top-level) | **maintainer** | backend / verification notes — distinct from the upstream Tiny Tapeout README Robert shipped |

## Commit history

All backend / verification commits are under GitHub user
`s99048100-code`. `src/project.v` is untouched; verify with:

```
git log --oneline src/project.v
git log --oneline --author="s99048100-code"
```

## Licence

All maintainer-authored material in this repo follows the same licence
as the upstream Tiny Tapeout project (`LICENSE`), and the sky130 PDK /
flow tooling are under their respective Apache-2.0 / open-source
licences.
